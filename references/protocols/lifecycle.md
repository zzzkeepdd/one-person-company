# 生命周期

## 创建销毁
- 辩论相关Agent（红队/蓝队/分析师/追责方/辩护方）：辩论开始创建，辩论结束销毁
- 验收相关Agent（code-qa/func-qa/整改协调）：每次验收创建，验收结束销毁
- 开发Agent（AI程序员/前端开发/开发执行）：每次任务创建，任务完成销毁

## 交接单
每个Agent销毁前生成交接单（见 handover.md）。包含四个字段。

## 禁止
- 不复用上次会话的Agent实例
- 不跨项目共享Agent角色
